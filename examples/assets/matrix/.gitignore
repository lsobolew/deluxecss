# Source frames for the matrix-frames demo, downscaled to 256px wide (exactly
# what the demo renders — the CLI's --resize 256 is then a no-op). Committed so
# CI can generate the demo. Anything else dropped here stays ignored.
*
!.gitignore
!matrix_*.png
